namespace MockDoor.Server.Constants;

internal static class HttpConstants
{
    /// <summary>
    /// The header key used to pass the base path to the UI
    /// Note: when editing this value remember to also edit it in the index.html script
    /// </summary>
    public const string CustomBasePathHeaderKey = "custom-base-path";
}